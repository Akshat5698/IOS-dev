IOS bootcamp :-

info.plist - info of the users - permissions
    notifications - 

    frontend
    backend
    developer.apple.com - syncing the both
    oops concept to access developer.apple.com
        Inheritence
            overriding - inheriting the prop of base class to inherited class
            overloading - same class having multiple methods with same name but different parameters
        polymorphism - 
        abstraction - Hiding the implementation details
        encapsulation - Binding the data and functions together

    API - Application Programming Interface
        editing... if else block seperate api for both the functions

    Riverpod = a clean way to manage and share state and dependencies throughout a Flutter application.

    xcode vs Flutter (difference)
        -sdk
            xcode - build inside the xcode
            flutter - 
        -ui
            xcode - UI is native
            flutter - UI is adaptive
        -debugging
            xcode - debugging tools are native
            flutter - debugging tools are adaptive
        -platform specific features  
            xcode - platform specific features are native
            flutter - platform specific features are adaptive